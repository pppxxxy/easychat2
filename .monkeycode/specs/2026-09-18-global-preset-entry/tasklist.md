# 需求实施计划

- [x] 1. 扩展 storage 与内置预设
  - [x] 1.1 在 `src/presets.js` 增加内置「角色状态」预设（id 为 `character-state`，提示词要求回复末尾附带心情、好感度与内心想法）（需求 4.1）
  - [x] 1.2 在 `src/storage.js` 新增 `getMemorySummarySettings()` / `saveMemorySummarySettings({ enabled, threshold })`，键为 `@easychat2_memory_summary`，阈值只接受大于 0 的整数，非法回退 40（需求 5.2、5.3；设计「数据模型」、「正确性属性」3）
  - [ ]* 1.3 属性测试：记忆总结阈值归一化与非法回退（设计「正确性属性」3）

- [x] 2. 新增 PresetPanel 组件
  - [x] 2.1 新建 `src/PresetPanel.js`，props 为 `{ visible, onClose }`，`visible` 变为真时加载预设、开关与记忆总结设置（设计「组件与接口」）
  - [x] 2.2 渲染预设列表：名称、描述与启用开关，开关切换即时保存（需求 2.2、2.5）
  - [x] 2.3 新增与编辑弹窗（名称、描述、提示词）以及删除二次确认（需求 2.3、2.4）
  - [x] 2.4 列表之外渲染「记忆总结」开关与阈值输入，非法输入回退 40 并提示（需求 5.1、5.2）
  - [ ]* 2.5 渲染脚本：列表、开关、增删改、记忆总结开关与阈值（设计「测试策略」）

- [ ] 3. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 4. SettingsScreen 迁移到全局配置入口
  - [x] 4.1 移除内联预设卡片与编辑弹窗及相关状态与处理函数（设计「组件与接口」）
  - [x] 4.2 新增「全局配置」卡片与「全局预设」入口行，副标题显示已开启数量或「未开启」（需求 1.1、1.2、1.3、1.4）
  - [x] 4.3 点击入口打开 `PresetPanel`，开启数量随开关变化刷新（需求 2.1）
  - [ ]* 4.4 脚本：入口副标题计数与「未开启」显示（设计「测试策略」）

- [x] 5. CharacterScreen 增加全局预设入口
  - [x] 5.1 在基本信息卡片附近新增「全局预设」入口行（需求 3.1）
  - [x] 5.2 打开与设置页相同的 `PresetPanel`，关闭后面板不改变未保存表单（需求 3.2、3.3；设计「正确性属性」4）
  - [ ]* 5.3 脚本：点击打开面板、关闭后表单不变（设计「测试策略」）

- [x] 6. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 7. 回归验证
  - [x] 7.1 运行 `npx expo export --platform android` 验证打包成功（设计「测试策略」）
  - [x] 7.2 同步 `.monkeycode/docs/` 中预设、设置页与角色页相关章节（需求 1、2、3、4、5）