# 需求实施计划

- [x] 1. 本地启发式与阈值
  - [x] 1.1 `src/moments/affinity.js`：关键词表与 `evaluateTurn` 产出 `{ delta, milestone }`，`clampAffinity` 夹在 ±100（需求 2.2、3 术语表）
  - [x] 1.2 `src/moments/moments.js`：`shouldTrigger` 覆盖好感上下限、轮次 50/100、特殊大事与去重（需求 2.3—2.7）
  - [x] 1.3 `buildMomentText` 基于触发类型与角色的固定模板生成文本，无模型调用（需求 5.1、5.2）
  - [x] 1.4 `appendMoment` 按 `MAX_MOMENTS`（200）淘汰最旧（设计「组件与接口」）

- [x] 2. 检查点 - 确保所有可运行验证通过

- [x] 3. 存储
  - [x] 3.1 `@easychat2_moments_settings`：`{ enabled }`（需求 1.1、1.4）
  - [x] 3.2 `@easychat2_moments`：动态数组，读取时规范化为倒序（需求 3.1）
  - [x] 3.3 `@easychat2_affinity`：按角色的 `{ score, turnCount, triggers }`（需求 2.1、2.7）

- [x] 4. 聊天触发
  - [x] 4.1 助手回复完成后评估好感与轮次，必要时生成动态并落库（需求 2.2—2.6）
  - [x] 4.2 开关关闭时不生成新动态，既有数据保留（需求 1.3）
  - [x] 4.3 触发流程不产生额外网络请求，且不阻塞消息状态（需求 2.2、5.1）

- [x] 5. 界面
  - [x] 5.1 设置页新增「虚拟朋友圈」开关（需求 1.1、1.4）
  - [x] 5.2 扩展页在开启时新增「朋友圈」分段（需求 1.2）
  - [x] 5.3 时间线倒序展示角色名、头像、文本、时间、点赞与评论，含空状态（需求 3.1—3.5）
  - [x] 5.4 点赞/取消点赞幂等（需求 4.1、4.2）
  - [x] 5.5 评论追加与首次评论后角色点赞（需求 4.3、4.5）
  - [x] 5.6 删除动态（需求 4.4）

- [x] 6. 检查点 - 确保所有可运行验证通过

- [x] 7. 回归与文档
  - [x] 7.1 脚本验证：好感 delta/milestone、阈值边界与去重、模板文本、淘汰策略、设置与状态存储规范化（设计「测试策略」）
  - [x] 7.2 打包验证 `npx expo export --platform android`
  - [x] 7.3 文档同步