# 需求实施计划

- [x] 1. 会话模型扩展与群聊创建
  - [x] 1.1 `sessionLibrary` 的 `normalizeSession` 增加 `type`（single/group）、`members`、`name`；`createEmptySession` 设为 single（设计「数据模型」）
  - [x] 1.2 新增 `createGroupSession(members, name, sessions)`（设计「组件与接口」）
  - [x] 1.3 `storage.createGroupSession(members, name)`：写入群聊会话并设为当前（需求 1.4；设计「组件与接口」）
  - [ ]* 1.4 单元脚本：群聊字段规范化、创建、克隆保留成员（设计「测试策略」）

- [x] 2. 群聊逻辑模块
  - [x] 2.1 新建 `src/groupChat.js`：`parseMentions(text, characters)` 解析 `@角色名`（需求 4.1）
  - [x] 2.2 `selectSpeakers`：调用 LLM 选 1-3 个发言角色，限制上限，失败回退本地规则（需求 3.1、3.2、3.3；设计「正确性属性」1、3）
  - [x] 2.3 `generateOpening`：生成群场景开场白与首位发言角色（需求 6.1）
  - [x] 2.4 `buildGroupHistory` 与 `buildGroupRequest`：为发言角色构造含发言者前缀历史的请求（需求 5.1、5.2、5.3）
  - [ ]* 2.5 单元脚本：`@` 命中、解析回退、历史前缀（设计「测试策略」）

- [x] 3. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 4. CharacterScreen 创建群聊
  - [x] 4.1 角色库卡片新增「群聊」按钮，打开多选面板（需求 1.1）
  - [x] 4.2 多选 2-8 个角色、群名可留空；不足 2 或超过 8 阻止并提示（需求 1.2、1.3；设计「错误处理」）
  - [x] 4.3 创建后刷新会话并切换到聊天页（需求 1.4）

- [x] 5. ChatScreen 群聊模式
  - [x] 5.1 识别群聊会话，顶部展示群名与群图标（需求 2.2、6.2）
  - [x] 5.2 发送时解析 `@` 并调度发言角色，逐个以各自角色卡设定回复（需求 3.1、4.1、4.2、5.1、5.2）
  - [x] 5.3 单角色失败生成错误气泡，其余角色继续（设计「错误处理」）
  - [x] 5.4 空群聊生成开场白并作为首条消息（需求 6.1）
  - [x] 5.5 消息渲染展示发言者头像与名字；用户消息与单聊一致（需求 6.2、6.3）
  - [ ]* 5.6 生命周期脚本：逐角色追加、错误继续、迟到丢弃（设计「测试策略」）

- [x] 6. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 7. MemoryScreen 群聊行
  - [x] 7.1 群聊行展示叠放成员头像与群名（需求 2.2）
  - [x] 7.2 群聊支持置顶、克隆与删除（沿用现有操作）（需求 2.3）

- [x] 8. 回归验证
  - [x] 8.1 运行 `npx expo export --platform android` 验证打包成功（设计「测试策略」）
  - [x] 8.2 同步 `.monkeycode/docs/` 中会话模型、聊天页、角色页与记忆页相关章节（需求 1—6）