# 需求实施计划

- [x] 1. ScrollScrubber 组件
  - [x] 1.1 新建 `src/ScrollScrubber.js`，props 为 `{ visible, onClose, messageCount, previews, onSeek, onToStart, onToEnd }`（设计「组件与接口」）
  - [x] 1.2 渲染竖向轨道与滑块，用 `PanResponder` 拖动，按滑动比例映射消息索引（需求 2.1；设计「正确性属性」2）
  - [x] 1.3 轨道上方「回到开头」、下方「回到最新」按钮（需求 1.3、3.1、3.2）
  - [x] 1.4 松手时以映射索引调用 `onSeek`（需求 2.2；设计「正确性属性」1）
  - [x] 1.5 消息数超过阈值时拖动显示预览卡（日期、发言者、缩略）；无消息时禁用（需求 2.3、3.3）
  - [ ]* 1.6 单元脚本：不同消息数与滑动比例下的索引映射（设计「测试策略」）

- [x] 2. ChatScreen 接线
  - [x] 2.1 顶部栏新增「定位」图标，打开与关闭滑动条；无消息时禁用（需求 1.1、1.2、3.3）
  - [x] 2.2 构建预览数据（时间、发言者、缩略）并传入组件（需求 2.3；设计「组件与接口」）
  - [x] 2.3 实现 `onSeek`：按索引用已记录偏移滚动定位，偏移缺失时回退（需求 2.2；设计「正确性属性」4）
  - [x] 2.4 `onToStart` 滚动到第一条，`onToEnd` 滚动到最新（需求 3.1、3.2）
  - [ ]* 2.5 生命周期脚本：超长列表映射精度、偏移缺失回退（设计「测试策略」）

- [x] 3. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 4. 回归验证
  - [x] 4.1 运行 `npx expo export --platform android` 验证打包成功（设计「测试策略」）
  - [x] 4.2 同步 `.monkeycode/docs/` 中聊天页相关章节（需求 1、2、3）