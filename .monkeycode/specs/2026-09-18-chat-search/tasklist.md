# 需求实施计划

- [x] 1. 搜索存储与定位目标
  - [x] 1.1 `src/storage.js` 新增 `searchMessages(keyword)`：批量读取全部会话消息，做不区分大小写的子串匹配，返回 `{ sessionId, characterId, messageId, role, text, updatedAt }` 列表（需求 1.2、3.1、3.2；设计「组件与接口」）
  - [x] 1.2 结果按会话 `updatedAt` 从新到旧排序（需求 1.4；设计「正确性属性」1）
  - [x] 1.3 `src/context/AppContext.js` 增加 `pendingTarget`（`{ sessionId, messageId }`）与 `setPendingTarget`、`consumePendingTarget`（设计「组件与接口」）
  - [ ]* 1.4 单元脚本：中文子串、大小写不敏感、空关键词返回空（设计「测试策略」）

- [x] 2. SearchScreen 结果页
  - [x] 2.1 新建 `src/SearchScreen.js`：全屏 Modal，含关键词输入与结果列表（角色名、会话摘要名、文本片段、时间）（需求 1.1、1.3）
  - [x] 2.2 空结果展示提示；关键词为空不搜索（需求 1.6、3.3）
  - [x] 2.3 点击结果：`setPendingTarget` + `switchCharacter` + `switchSession`，关闭结果页并切换到聊天页（需求 1.5；设计「正确性属性」5）
  - [ ]* 2.4 渲染脚本：结果条目、空状态、点击回调（设计「测试策略」）

- [x] 3. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 4. MemoryScreen 搜索入口
  - [x] 4.1 记忆页顶部新增搜索入口，打开 `SearchScreen`（需求 1.1）

- [x] 5. ChatScreen 定位与高亮
  - [x] 5.1 记录每条消息的布局偏移，供定位使用（设计「组件与接口」）
  - [x] 5.2 消费 `pendingTarget`：消息加载完成后滚动定位并高亮；目标不存在时回退到底部（需求 1.5；设计「错误处理」、「正确性属性」2）
  - [x] 5.3 会话内搜索条：顶部栏入口、输入后标记全部命中、显示第 x/n 条并支持上一个/下一个导航（需求 2.1、2.2、2.3、2.4）
  - [x] 5.4 关闭搜索清除高亮（需求 2.5）
  - [x] 5.5 高亮渲染：用户消息对关键词子串染色，助手消息以气泡高亮标记当前与普通命中（需求 2.2；设计「正确性属性」4）
  - [ ]* 5.6 生命周期脚本：匹配计数、导航索引、定位回退（设计「测试策略」）

- [x] 6. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 7. 回归验证
  - [x] 7.1 运行 `npx expo export --platform android` 验证打包成功（设计「测试策略」）
  - [x] 7.2 同步 `.monkeycode/docs/` 中记忆页、聊天页与持久化相关章节（需求 1、2、3）