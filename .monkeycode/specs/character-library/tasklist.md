# 需求实施计划

- [ ] 1. 扩展 storage.js 支持角色库持久化
  - [ ] 1.1 新增存储键与默认值：定义 `@easychat2_characters`、`@easychat2_active_character`，并在 `DEFAULT_CHARACTER` 增加 `lastUsedAt`（需求 8.1；设计「数据模型」）
  - [ ] 1.2 实现 `getCharacterLibrary`：库键缺失时用旧键 `@easychat2_character` 迁移，并补入默认角色（需求 6.1、6.3、8.2、8.3、8.4）
  - [ ] 1.3 实现 `saveCharacterLibrary`、`getActiveCharacterId`、`setActiveCharacterId`（需求 7.1、8.1）
  - [ ] 1.4 实现 `getActiveCharacter`：当前 id 不在库中时回退默认并修正持久化 id（需求 7.3）
  - [ ] 1.5 实现 `upsertCharacter` 与 `deleteCharacter`（删除角色时一并删除其消息键）（需求 3.1、5.2、5.3）
  - [ ] 1.6 属性测试：角色库读取后必含默认角色，且库键存在时不再读旧键（属性 1、6；需求 6.1、6.3、8.4）
  - [ ] 1.7 属性测试：任意无效当前角色 id 均回退默认角色（属性 2；需求 7.3）
  - [ ] 1.8 属性测试：消息仅以角色 id 为键读写，删除角色后消息键被移除（属性 3；需求 2.1、5.3）
  - [ ] 1.9 属性测试：列表按 `lastUsedAt` 降序、并列按 id 升序（属性 5；需求 1.6）
  - [ ] 1.10 属性测试：解析失败时回退为仅含默认角色的库（属性 1；需求 8.2）

- [ ] 2. 改造 AppContext 为角色库状态
  - [ ] 2.1 建立状态与派生值：`characters`、`activeId`、`character`、`loaded`，保留 `characterRef`/`loadedRef` 模式（设计「AppContext」）
  - [ ] 2.2 实现 `updateCharacter(patch)`：更新当前角色并持久化，失败回滚后抛出（需求 4.1、4.3）
  - [ ] 2.3 实现 `switchCharacter(id)`：更新 `lastUsedAt`、持久化当前 id（需求 1.7、7.1）
  - [ ] 2.4 实现 `addCharacter(character)`：以唯一 id 入列并设为当前角色（需求 3.1、3.2、3.3、8.1）
  - [ ] 2.5 实现 `deleteCharacter(id)`：非默认角色删除并切换当前，默认角色拒绝删除（需求 5.2、5.4、5.5）
  - [ ] 2.6 属性测试：任何写入路径均不落盘 `pending` 消息（属性 4；需求 2.2）
  - [ ] 2.7 单元测试：写入失败时内存状态回滚且向上抛出（设计「错误处理」）

- [ ] 3. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [ ] 4. 角色页陈列与切换
  - [ ] 4.1 渲染角色列表：按最近使用降序、显示选中标记（需求 1.1、1.3、1.6）
  - [ ] 4.2 点选条目调用 `switchCharacter`（需求 1.2、1.4）
  - [ ] 4.3 新建空白角色与导入角色卡接入 `addCharacter`（需求 3.1、3.2、3.3）
  - [ ] 4.4 编辑区保存当前角色走 `updateCharacter`（需求 4.1、4.2、4.3）
  - [ ] 4.5 非默认角色删除入口与二次确认（需求 5.1、5.2、5.4、5.5）
  - [ ] 4.6 单元测试：列表排序助手与新增角色 id 唯一（属性 5、7；需求 1.6、3.4）

- [ ] 5. 聊天页顶部切换入口
  - [ ] 5.1 顶部展示当前角色名称（需求 1.5）
  - [ ] 5.2 点击弹出 Modal 角色列表并点选切换（需求 1.5、1.2）
  - [ ] 5.3 切换时中断进行中的生成、丢弃迟到回复并加载目标会话（需求 1.4、2.3、2.4）
  - [ ] 5.4 属性测试：生成中切换会中断请求且迟到回复被丢弃（属性 8；需求 1.4、2.3）

- [ ] 6. 检查点 - 确保所有测试通过
  - 确保所有测试通过,如有疑问请询问用户

- [ ] 7. 回归验证
  - [ ] 7.1 运行既有 25 项用例并补充角色库相关用例，全部通过（设计「测试策略」）
  - [ ] 7.2 运行 `expo export --platform android` 验证打包成功（设计「测试策略」）