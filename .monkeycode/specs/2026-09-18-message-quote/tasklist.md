# 需求实施计划

- [x] 1. 数据与请求注入
  - [x] 1.1 `chatPipeline.buildRequestMessages` 新增 `quote` 参数，在用户消息文本前追加 `[引用<名称>的消息] <文本>` 强调段（需求 3.1、3.2）
  - [x] 1.2 无引用时保持原有请求结构；引用只作用于当前用户消息（需求 3.3、4.2）
  - [x] 1.3 带图片时多模态文本同样包含引用前缀（需求 1.5）
  - [x] 1.4 `groupChat.buildGroupRequest` 透传 `quote`，群聊同样适用（需求 3.4）

- [x] 2. 检查点 - 确保所有可运行验证通过

- [x] 3. 发起引用
  - [x] 3.1 消息操作行新增「引用」按钮（需求 1.1）
  - [x] 3.2 输入区上方展示引用块，含发送者与文本摘要，并提供取消（需求 1.2、1.3）
  - [x] 3.3 `buildQuotePayload` 截断摘要至 `QUOTE_TEXT_MAX`（200 字）（需求 2.2）
  - [x] 3.4 发送时把 `quoted` 字段写入用户消息并清空引用目标（需求 1.4）
  - [x] 3.5 切换角色时清空引用与附件（需求 2 边界）

- [x] 4. 引用展示与定位
  - [x] 4.1 消息气泡内正文之上渲染引用块（需求 2.1）
  - [x] 4.2 点击引用块复用会话内定位滚动到原消息（需求 2.3）
  - [x] 4.3 原消息不存在时提示「原消息已删除」且不报错（需求 2.4）

- [x] 5. 检查点 - 确保所有可运行验证通过

- [x] 6. 回归与文档
  - [x] 6.1 脚本验证：有/无引用、缺名称回退、空文本不注入、多模态、历史不受影响（设计「测试策略」）
  - [x] 6.2 打包验证 `npx expo export --platform android`
  - [x] 6.3 文档同步