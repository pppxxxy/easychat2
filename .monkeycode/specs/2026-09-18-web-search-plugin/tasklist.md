# 需求实施计划

- [x] 1. 插件存储模型
  - [x] 1.1 `src/storage.js` 新增键 `@easychat2_plugins` 与内置插件默认值（`web-search`）及规范化（需求 1.1；设计「数据模型」）
  - [x] 1.2 实现 `getPlugins` / `savePlugins` / `getEnabledPlugins`，内置项缺失时补入（设计「组件与接口」）
  - [ ]* 1.3 单元脚本：默认播种、开关持久化、规范化（设计「测试策略」）

- [x] 2. 插件运行时
  - [x] 2.1 新建 `src/plugins/webSearch.js`：`runWebSearch({ query, config })` 适配 serpapi / google-cse / bing / custom，XHR 调用并在 10 秒超时（需求 2.1、3.1；设计「组件与接口」）
  - [x] 2.2 新建 `src/plugins/registry.js`：内置触发词检测、`runPlugins` 遍历启用插件、同一会话 30 秒频率限制、格式化背景资料（需求 3.1、3.4；设计「正确性属性」4、5）
  - [x] 2.3 未配置密钥或 Provider 时跳过，失败/超时静默降级（需求 2.4、3.5；设计「错误处理」）
  - [ ]* 2.4 单元脚本：触发词命中/未命中、密钥缺失跳过、失败降级（设计「测试策略」）

- [x] 3. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 4. 请求注入
  - [x] 4.1 `src/chatPipeline.js` 的 `buildRequestMessages` 增加可选 `pluginContext`，追加 `[背景资料]` 段（设计「组件与接口」）
  - [x] 4.2 `src/ChatScreen.js` 发送前调用 `runPlugins` 并把结果传入 `buildRequestMessages`（需求 3.1、3.2；设计「组件与接口」）
  - [x] 4.3 注入内容含标题、来源链接与获取时间；密钥不进入提示词（需求 3.3、4.1；设计「正确性属性」2）
  - [ ]* 4.4 脚本：含/不含插件上下文时的消息序列（设计「测试策略」）

- [x] 5. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 6. 插件管理界面
  - [x] 6.1 新建 `src/PluginPanel.js`：插件列表与启用开关、Provider 选择、密钥（密文展示）、`cx`、结果条数与保存（需求 1.2、1.3、2.1、2.2、2.3）
  - [x] 6.2 `src/SettingsScreen.js` 在「全局配置」分组新增「插件」入口，副标题显示已开启状态（需求 1.2）
  - [x] 6.3 开启但未填密钥时提示先填写（需求 2.4）
  - [ ]* 6.4 渲染脚本：开关、Provider 切换、密钥密文、保存（设计「测试策略」）

- [x] 7. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 8. 回归验证
  - [x] 8.1 运行 `npx expo export --platform android` 验证打包成功（设计「测试策略」）
  - [x] 8.2 同步 `.monkeycode/docs/` 中插件、设置页与请求组装相关章节（需求 1—4）