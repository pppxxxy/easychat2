# 需求实施计划

- [x] 1. 来源多模型与存储迁移
  - [x] 1.1 `normalizeApiConfig` 扩展为 `models` / `activeModel` / `supportsThinking` / `supportsVision`，并把旧 `model` 迁移为单元素模型列表（需求 1.1、1.2；设计「数据模型」）
  - [x] 1.2 新增 `getActiveModel(config)` 并在 `api.js` 使用，替代直接读 `model`（设计「组件与接口」）
  - [x] 1.3 `models` 始终非空、`activeModel` 始终属于 `models`，非法时回退（设计「正确性属性」2）
  - [ ]* 1.4 单元脚本：迁移、回退、幂等（设计「测试策略」）

- [x] 2. SettingsScreen 模型列表与能力确认
  - [x] 2.1 模型区改为列表：输入添加、点击设为当前、可删除；至少保留一个（需求 1.3、1.4）
  - [x] 2.2 「检测模型」结果加入列表并设为当前（需求 1.3）
  - [x] 2.3 保存前弹出「是否支持思考 / 是否支持识图」确认并写入能力标记（需求 3.1、3.2、3.3）
  - [x] 2.4 配置摘要展示当前模型（需求 1.1）
  - [ ]* 2.5 界面脚本：模型增删选、能力确认写入（设计「测试策略」）

- [x] 3. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 4. ChatScreen 模型切换面板
  - [x] 4.1 顶部栏新增「模型」入口（需求 2.1）
  - [x] 4.2 面板先列来源再列模型，标出当前来源与当前模型（需求 2.2、2.4）
  - [x] 4.3 选择模型后更新该来源当前模型并持久化，后续请求生效（需求 2.3）
  - [ ]* 4.4 界面脚本：两级选择与持久化（设计「测试策略」）

- [x] 5. 回归验证
  - [x] 5.1 运行 `npx expo export --platform android` 验证打包成功（设计「测试策略」）
  - [x] 5.2 同步 `.monkeycode/docs/` 中 API 配置与聊天页相关章节（需求 1—4）