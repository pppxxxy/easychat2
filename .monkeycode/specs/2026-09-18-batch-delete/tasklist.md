# 需求实施计划

- [x] 1. 批量删除存储与状态
  - [x] 1.1 `src/storage.js` 新增 `deleteSessions(sessionIds)`：批量移除多个会话的元数据，并 `multiRemove` 其消息键（需求 2.4；设计「组件与接口」）
  - [x] 1.2 `src/context/AppContext.js` 新增 `deleteSessions(ids)`：校验加载完成，若包含当前会话先 `startNewSession` 再删除，保证 `activeSessionId` 有效（需求 2.5；设计「正确性属性」2）
  - [x] 1.3 批量删除失败时回滚并抛出，不产生部分删除（需求 3.3；设计「错误处理」）
  - [ ]* 1.4 单元脚本：批量删除元数据与消息、删除当前会话后指针有效（设计「测试策略」）

- [x] 2. MemoryScreen 编辑模式
  - [x] 2.1 顶部新增「编辑」入口，仅在存在会话时显示；进入后替换为「完成」（需求 1.1、1.2、3.1）
  - [x] 2.2 编辑模式下每行显示勾选框，点击行切换选中且不打开会话（需求 1.3、2.1）
  - [x] 2.3 底部固定操作条显示「全选」与「删除（N）」，未勾选时删除禁用；全选可切换为取消全选（需求 1.4、2.2、3.2、3.4）
  - [x] 2.4 删除前二次确认并说明数量，确认后调用 `deleteSessions` 并退出编辑模式（需求 2.3、2.6）
  - [x] 2.5 删除失败时 `Alert` 并保持列表与勾选状态（需求 3.3；设计「错误处理」）
  - [ ]* 2.6 渲染脚本：编辑模式、全选、计数、删除确认、空状态（设计「测试策略」）

- [x] 3. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 4. 回归验证
  - [x] 4.1 运行 `npx expo export --platform android` 验证打包成功（设计「测试策略」）
  - [x] 4.2 同步 `.monkeycode/docs/` 中记忆页与会话状态相关章节（需求 1、2、3）