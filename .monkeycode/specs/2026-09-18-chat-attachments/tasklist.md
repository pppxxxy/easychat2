# 需求实施计划

- [x] 1. 附件模块
  - [x] 1.1 新建 `src/attachments.js`：`TEXT_EXTENSIONS`、`isTextLike`、`isImage`（需求 1.2、2.2）
  - [x] 1.2 `pickAttachment`、`readTextAttachment`（含大小上限）、`readImageDataUri`（需求 1.3、1.4、2.1）
  - [x] 1.3 `mergeTextAttachments` 把文本附件并入用户消息上下文（需求 1.3）
  - [ ]* 1.4 单元脚本：扩展名判定、合并（设计「测试策略」）

- [x] 2. 多模态请求
  - [x] 2.1 `src/chatPipeline.js` 的 `buildRequestMessages` 支持 `images`，把最后一条用户消息转为多模态数组（设计「组件与接口」）
  - [x] 2.2 无图片时保持纯文本（设计「正确性属性」1）
  - [ ]* 2.3 单元脚本：有无图片的消息序列（设计「测试策略」）

- [x] 3. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 4. ChatScreen 附件与全屏输入
  - [x] 4.1 输入栏附件入口，弹出「纯文本文档 / 图片」选择（需求 1.1、2.1）
  - [x] 4.2 文本附件读取内容；不支持类型与超限给出提示（需求 1.2、1.4、1.5）
  - [x] 4.3 图片仅在当前来源 `supportsVision` 时允许，否则提示（需求 2.2、2.3）
  - [x] 4.4 已选附件以标签与缩略图展示并可移除（需求 2.4）
  - [x] 4.5 发送时合并文本附件并传递图片；仅附件也可发送（需求 1.3、2.2）
  - [x] 4.6 输入栏最右新增全屏输入入口，全屏界面提供发送与右上角关闭，退出保留文本（需求 3.1、3.2、3.3、3.4）
  - [ ]* 4.7 界面脚本：附件标签与移除、全屏开关与发送（设计「测试策略」）

- [x] 5. 检查点 - 确保所有可运行验证通过
  - 确保所有验证通过,如有疑问请询问用户

- [x] 6. 回归验证
  - [x] 6.1 运行 `npx expo export --platform android` 验证打包成功（设计「测试策略」）
  - [x] 6.2 同步 `.monkeycode/docs/` 中聊天页与请求组装相关章节（需求 1—4）