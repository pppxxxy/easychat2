# 需求实施计划

- [x] 1. 依赖与打包
  - [x] 1.1 安装 `expo-speech` 与 `expo-av`（SDK 50 兼容）（设计「依赖（新增）」）
  - [x] 1.2 `npm ci --dry-run` 确认锁文件同步

- [x] 2. 声明式适配
  - [x] 2.1 `src/tts/providers.js`：内置系统引擎与小米 MiMo / 硅基流动 / 讯飞星火 / 阶跃星辰 / 腾讯云 / 阿里云 / 百度智能云 / 火山引擎 / MiniMax 共十个播报源（需求 2.4）
  - [x] 2.2 声明覆盖接口地址、方法、鉴权（header/query/body/token）、文本/音色/语速/格式字段、响应模式（binary/base64）、`signer` 与 `fields`（需求 3.1、3.2、3.4）
  - [x] 2.3 支持令牌兑换与内存缓存（按 `tokenTtlSec` 过期），以及讯飞/腾讯云/火山签名标识（需求 3.3、3.4）

- [x] 3. 检查点 - 确保所有可运行验证通过

- [x] 4. 存储与设置面板
  - [x] 4.1 `storage.js` 新增 `getTtsSettings` / `saveTtsSettings`（键 `@easychat2_tts`，默认 `enabled:false`、`activeProvider:'system'`）（需求 2.5）
  - [x] 4.2 `src/TtsPanel.js`：播报源选择 + 由 `fields` 驱动的字段表单（需求 2.1、2.2、2.3）
  - [x] 4.3 设置页「全局配置」新增「语音播报」横档入口（需求 2.1）

- [x] 5. 播报控制
  - [x] 5.1 聊天页顶部栏常驻播报开关，切换即持久化并显示开启/关闭状态（需求 1.1、1.4、1.5）
  - [x] 5.2 助手回复完成且开关开启时自动播报（需求 1.2、4.1）
  - [x] 5.3 助手消息提供「播报」手动重播入口（需求 5.2）
  - [x] 5.4 发送新消息前停止播报，切换开关关闭时停止（需求 1.3、5.1）
  - [x] 5.5 系统引擎走 `expo-speech`，云端音频走 `expo-av`；不可用时给出可读提示（需求 4.1、4.3、5.3）
  - [x] 5.6 文本按 `TTS_MAX_CHARS` 截断（需求 5.4）

- [x] 6. 检查点 - 确保所有可运行验证通过

- [x] 7. 回归与文档
  - [x] 7.1 脚本验证：Provider 完整性与字段结构、文本截断、错误映射、请求构造（Bearer/query/token/签名）、设置默认与回退（设计「测试策略」）
  - [x] 7.2 打包验证 `npx expo export --platform android`（重点验证新增原生依赖）
  - [x] 7.3 文档同步