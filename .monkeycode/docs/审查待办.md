# 代码审查待办

审查基线：`main@c156b9d` 之后的当前未提交工作树。提交与推送均暂停。

## 已确认并已处理

- [x] 角色页外部更新同步与未保存内容导出/制卡
- [x] 制卡草稿大字段、集合与文件载荷持久化
- [x] 制卡 AI 清空字段、清空标签、导入锁、保存失败保留输入
- [x] 制卡清空/保存串行；清空以索引删除为提交点，旧载荷清理失败隔离
- [x] 角色页切换授权、保存归属与回滚保护
- [x] 摘要删除边界前消息时禁用对应世界书摘要，保留原内容可恢复
- [x] 世界书导出保留 role、matchWholeWords、useProbability、深度与扫描范围
- [x] 字符串备用开场白保持完整文本
- [x] 展示正则纯文本替换不改写脚本、样式与标签
- [x] 群聊助手消息按 speakerId 应用正则
- [x] 群聊保留用户人设、全局预设、摘要、引用与联网资料
- [x] 会话克隆重写嵌套引用
- [x] 损坏消息体禁止克隆；克隆元数据失败清理消息体
- [x] 删除当前群聊切换已有会话；批量删除避免预创建失败残留
- [x] 恢复孤儿会话支持大消息 SQLite fallback
- [x] 消息搜索按会话隔离大值读取失败
- [x] 删除 tombstone 恢复后允许继续写入
- [x] 角色库/activeId 部分提交补偿
- [x] 角色库阻断时跳过头像清理；保留动态头像引用
- [x] 摘要删除时清理世界书摘要并检测并发角色编辑
- [x] 已有会话摘要不因作用域降级消失
- [x] 向量维度不匹配不再截断比较；上下文总长度受限；向量请求支持取消
- [x] 联网搜索缓存区分配置；总超时约 10 秒；支持 AbortSignal；上下文隔离与限长
- [x] TTS 二进制响应按 ArrayBuffer 处理；停止/并发播报使用 generation token
- [x] 配置面板加载门控、保存队列、阈值重复提交锁
- [x] 搜索页面旧请求不会覆盖新请求
- [x] 动态回复切换扩展时取消；评论保存失败保留草稿
- [x] 游戏 DOM Storage、扩展动态空分段、章节图片页码重置
- [x] 角色导入硬上限与参考图大小/像素限制
- [x] 启动流程门控与迁移失败重试；错误信息脱敏
- [x] 密钥重叠值按最长优先脱敏
- [x] 大 HTML 视口内联样式识别、viewport meta、超高内容内部滚动
- [x] GreetingPicker、SessionRecovery、角色创建失败路径保留可重试状态
- [x] API 配置切换后丢弃旧来源回复；SSE DONE 与解析失败主动中止 XHR
- [x] 搜索结果与记忆会话列表使用 FlatList，降低全量 ScrollView 挂载压力
- [x] 生图设置保存串行化，模型候选取当前值，取消检测不触发付费探测
- [x] TTS 设置保存串行化，旧请求失败不会回滚新编辑
- [x] 向量、TTS、联网搜索请求的 AbortSignal 与定时器清理
- [x] 富 HTML 卡片命令增加用户确认，WebView 禁止多窗口自动打开

## 旧模型时代存活领地复核（37 文件逐行）

> 对 302 个旧提交的存活领地（37 个从未再被改过的文件，约 2,900 行）按风险排序逐行细查。

- [x] **P1｜MiniMax TTS 密钥从不发送**：声明表 `auth.type='query'` 只把 GroupId 拼进 URL，用户填的 API Key（Bearer Token）在请求头/查询串/请求体三处都不出现，请求必然 401。已新增 `auth.bearer` 支持：query 认证之外同时发 `Authorization: Bearer`。
- [x] **P1｜阿里云 TTS AppKey 收集后丢弃**：声明表收集 AppKey，但既无 `appIdField`（body 注入）也无任何 query 注入 → AppKey 从不发送，NLS 网关必然拒绝。已新增 `queryFields` 声明与装配逻辑，appkey 拼进查询串。
- [x] **P1｜腾讯云把 SecretKey 明文当 Authorization 头发送**：signer='tencent' 未实现 TC3-HMAC-SHA256 签名，buildHeaders 却把 SecretKey 原样塞进 Authorization 头——必然 401 且密钥明文暴露在头里。已改为 fail-fast 明确报错，绝不发送原始密钥。
- [x] **P1｜讯飞 wss 端点产生难懂错误**：XHR 无法打开 wss://，原来抛难懂的 XHR 网络错误。已 fail-fast 明确提示"WebSocket 协议暂不支持"。
- [x] **P1｜小米 MiMo 域名错误**：原配置 `api.xiaomi.com/v1/audio/speech` 域名错误。经核实 `platform.xiaomimimo.com`（官方开放平台）与 `api.xiaomimimo.com`（API 域名）均真实存在；此前一轮我误判为"端点臆造"，实际只是域名写错。已改为 `https://api.xiaomimimo.com/v1/audio/speech`（官方兼容端点），鉴权改 `api-key` 头（非 Bearer），响应改 `base64` + `message.audio.data`（兼容端点返回 JSON 而非音频二进制）。鉴权与响应字段按官方文档口径修正，与讯飞/腾讯一样待真实服务实测。
- [ ] **TTS 引擎把所有 base64 音频统一按 MP3 播放**：`speak()` 用 `data:audio/mp3` 硬编码 MIME，各供应商实际返回格式（wav/pcm/opus）未逐一实测，待真实服务验证后按供应商声明格式。

### 37 文件复核其余结论（已完成，无缺陷或已修复）

- [x] **P1｜联网搜索 Bing 供应商已停服**：Bing Search API 于 2025-08-11 停服（微软 2025-05 公告），新用户无法再开通密钥，应用却引导去 Azure 门户申请。已从 `plugins/providers.js` 与存储白名单移除；存量 bing 配置规范化回默认 provider。
- [x] **P2｜制卡编辑器标签上限漏改**：修复 forge.js 标签上限时，`CardForgeEditor.js` 的 `slice(0, 10)` 与"最多 10 个"提示是漏掉的兄弟调用点。已导出 `MAX_FORGE_TAG_COUNT` 统一为 100。
- [x] **imageGen/apiVendors/plugins 三张声明表与引擎装配全链路核实**：`{model}` 占位符有替换、数字路径有数组处理、Stability 空 `base64Field` 走字符串分支、缺 i2i 时 fail-fast；`authHeader/authScheme` 从声明一路传到聊天请求与模型检测；webSearch 的 extraFields（cx）/authPrefix/三种认证类型/嵌套路径/POST body 均正确装配。无缺陷。
- [x] **moments 三件套 / characterPresets / chatMedia / messageSelection / cardGreetings / vectorMemory scope+providers / characterIdentity**：逐行复核，边界处理正确、有既有测试覆盖，无缺陷。`appendMoment` 内部升序与读端降序重排自洽。
- [x] **games/games.js 模板字面量风险**：模板定义的两游戏（猜数字/贪吃蛇）内部无 `${` 与反引号；三个双引号定义的游戏内部模板字面量是字面字符安全送达 WebView。无缺陷。
- [x] **ui/ 八组件 + Chapter 三组件 + tokens/images/presets/微文件**：色键与主题定义全量交叉核对（`primaryAlpha` 为函数且调用正确）、无 map 渲染缺 key、style 均正确转发、外链均有二次确认。无缺陷。

## 老助手第二轮复核（对齐 2a05a32 的 8 条，对照当前 HEAD 逐条验证）

> 老助手清单基于 2a05a32（落后一笔提交）。验证结果：2 条对我 e→f 轮修复的有效质疑已修；1 条新竞态已修；3 条为已修/不成立的旧状态；2 条维持原判并补充依据。

- [x] **#1｜视口判定根选择器正则打在整条规则上**：属实（我的实现缺陷）。`url(/body.png)`、字体名 `"My body Font"` 里的裸词 body 会让非根规则误判为视口型。已改为先切出选择器部分（`{` 之前）再做根匹配；补三组误判回归用例。
- [x] **#2｜收窄过度漏判 `#app{position:fixed;inset:0}` 整屏卡**：属实（我在 e→f 轮把该用例改断言 false，固化了回归）。已新增“四边钉死”强信号：`inset:0` 或 top/right/bottom/left 全 0 的 fixed 容器无论选择器均为视口型（样式块与内联都覆盖，值终止符兼容 `;`/`}`/引号）；只钉两三边、带偏移的悬浮挂件仍不误判。`.x{position:fixed;inset:0}` 断言改回 true。
- [x] **#4｜主题初始加载无版本守卫**：属实（新竞态，与已修的失败回滚同类反向）。启动瞬间改主题时，晚到的磁盘回填会把界面拉回旧值。已在回填前比对 `persistRevisionRef`，期间已有 persist 则跳过回填。
- [~] **#3｜编辑器标签 slice(0,10)**：旧状态。`5d91440` 已导出 `MAX_FORGE_TAG_COUNT` 并在编辑器引用（上限 100，提示同步）。
- [~] **#5｜onClear 未重置摘要边界**：与当前代码不符。onClear 已调用 `invalidateHistorySummaries`（全部消息 id 作为 removedIds）统一失效。附带核实：`resetSessionSummaries` 确无生产调用点——它是被失效重构取代的旧入口，仍有测试覆盖、语义自洽，保留为公共存储 API。
- [~] **#6｜头像回收无 URI 归一化**：维持不可达判定。全部写入方（角色/群聊/资料三处）用同一 `documentDirectory + avatars/` 常量与结构化文件名拼接，存储引用与目录重建是同一字符串；且头像文件是用户相册原件的副本，误删也可从源头恢复。损坏守卫（profile/引用备份键/revision/TTL）已就位，防御性解码反而可能引入误匹配。
- [~] **#7｜导出只清 tEXt**：旧状态。`5d91440` 已同时清除 `tEXt`/`iTXt`/`zTXt` 的 chara/ccv3，并有 iTXt 往返回归测试。
- [~] **#8｜default.jpg 增大包体**：包体说法不成立——Metro 只打包被 require 的静态资源，导出产物资产清单无 default.jpg；它只增大 git 仓库体积。保留是 AGENTS.md 记录的项目约定（本地大卡测试素材）；如需仓库整洁可 `git mv` 至 tests/fixtures/，待用户决定。

**过程自查**：本轮还发现并修复了我自己在 e→f 轮犯的转写错误——重写 richHtml 测试块时把“实体转义代码示例不误判”用例抄成了未转义版本，导致该用例形同虚设；已恢复实体断言并重跑（视口判定现 15/15）。

## 老快照盲区复核（54f80b4→af428eb，51 提交，外部初判 8 条）

> 外部助手对 51 条盲区提交出具 8 条初判。逐条对照当前 HEAD（2a05a32）验证：1 条属实已修复；2 条为当前代码已修复的历史问题；1 条经写入方分析不可达；4 条为有意设计/项目约定。

- [x] **#4｜cardExporter 只清 tEXt 旧卡块**：属实。`injectCharaChunk` 现同时识别并移除 `tEXt`/`iTXt`/`zTXt` 三种文本块里的 `chara`/`ccv3`（关键词提取器通用），导出不再携带新旧双份卡数据；补 iTXt 往返回归测试。
- [x] **#1｜删除角色先清向量后写库，回滚不还原向量**：历史问题，当前 HEAD 已是正确顺序——`saveCharacterState` 先写角色库、activeId 失败内联回滚磁盘，**之后**才清向量、最后删消息键；写库失败时向量未动。审查基于旧快照，无需修改。
- [x] **#2｜清空聊天未重置摘要边界**：历史问题，当前 HEAD 已接线——`onClear` 调用 `invalidateHistorySummaries`（全部消息 id 作为 removedIds），统一失效路径会重置边界并清理摘要；群聊本不参与会话摘要（`countCharacterMemories` 排除 group），跳过正确。
- [~] **#3｜头像回收无 URI 归一化（%20/file:// 写法差异）**：不可达。所有 `avatars/` 文件均由应用写入，文件名为 `characterId-key-时间戳.ext` 结构化生成（无空格/特殊字符），存储引用与 `readDirectoryAsync` 重建是同一常量+文件名的字符串拼接，编码分歧无产生路径；损坏守卫（profile/引用备份键/revision/TTL）已就位。防御性解码反而可能引入误匹配，不采纳。
- [~] **#5｜生图默认重试 1→0**：有意收敛。付费生图仅在 `allowAutomaticRetries === true` 时重试，避免网络抖动重复扣费，与文档一致。
- [~] **#6｜制卡标签分隔符去掉空白**：有意收敛（cbbe6f4）。保留含空格标签（如 "final fantasy"）不再被拆碎，UI 提示明确写"用顿号或逗号分隔"；角色标签为描述性元数据，不参与匹配逻辑。
- [~] **#7｜向量/资料 500ms 防抖 + 卸载不 await**：卸载清理实际会冲刷——清除定时器后把快照投递进串行保存队列（React 清理函数无法 await，promise 在卸载后继续执行）。仅进程击杀窗口可丢最后 500ms 改动，旧即时写实现同样存在击杀窗口，属可接受折衷。
- [~] **#8｜default.jpg 死素材**：项目有意约定。AGENTS.md 明确"保留仓库用于本地大卡解析；应用不引用该文件"，用于手工大卡测试素材。如需仓库整洁可迁至 tests/fixtures/（git mv 保留单一 blob），待用户决定。

## e→f 差距复核（main@03765a6 → e8514f2 后的外部审查）

> 外部助手对 e→f 差距出具 10 条建议，逐条对照当前代码验证：9 条属实并已修复；第 10 条（审查文档入库）部分属实——文档入库是项目约定（AGENTS.md 要求 `.monkeycode/docs/` 随行为变更同步），保留文档，但自相矛盾的 CI 勾选已拆分订正。

- [x] **P1｜AI 制卡空字符串清空已有字段**：提示词要求模型回全量 JSON，模型给未改字段填 `""` 会抹掉用户长字段。已改为 null 哨兵协议：`parseCardPatch` 把 `null` 记为显式清空指令，`mergeDraft` 忽略空串/空数组，清空项在变更列表中标注（已清空）；两处提示词补充清空规则说明。
- [x] **P1｜空 tags 数组清空全部标签**：同上，空数组不再构成清空指令，清空标签必须 `tags: null`。
- [x] **P1｜动态回复旧 finally 清掉新请求的正在回复标记**：`replyingRef` 与 `setReplying` 的清理补上与 controller 相同的"只清自己这一次"守卫，去重不再失效，同一动态不会并发重复回复。
- [x] **P2｜主题保存失败回滚无版本校验**：`persistRevisionRef` 版本守卫——旧失败晚到不回滚新选择；A 失败 B 成功时界面与磁盘一致停在 B。成功快照仍按队列顺序无条件推进（它必须等于磁盘真实值）。
- [x] **P2｜标签上限两套标准往返截断**：`MAX_TAG_COUNT=10` 与 `MAX_PRESERVED_ITEMS=2000` 收敛为单一 `MAX_FORGE_TAG_COUNT=100`，草稿导入、提示词投影、补丁解析、合并四处统一。
- [x] **P2｜生图结果因配置变更被静默丢弃**：修订守卫命中时明确弹窗"生成期间图源设置已变更，本次结果不再保留"，已付费结果不再凭空消失。
- [x] **P2｜参考图校验并入主 try 扩大失败面**：大小/像素校验独立 try，`content://`、`ph://`、HEIC 上校验抛错只降级跳过，不阻断选图。
- [x] **P2｜内联 position:fixed 泛化误判视口型**：`position:fixed` 只在根级规则（html/body/:root 选择器或根标签内联样式）生效；普通卡片 fixed 挂件/角标不再把整条消息推入不可交互路径。视口单位（100vh 等）保持任意元素强信号。
- [x] **P2｜动态页摘要作用域调用未升级**：`isSessionScopedMemory` 补传动态来源会话与其消息，与 ChatScreen 同口径，记忆写入通道不再分叉。
- [x] **P2｜审查文档勾选与正文矛盾**：CI 条目已拆分——EAS CLI 固定标 [x]，actions SHA 固定单列 [ ]；文档按项目约定保留在仓库。
- [~] **审查文档应移出仓库**：不采纳。AGENTS.md 明确要求 `.monkeycode/docs/` 随行为变更同步，文档入库是项目约定；仅订正了与代码矛盾的勾选。

## 审核助手复核（main@03765a6）

> 外部审核提出 8 条建议，逐条对照代码验证：1-7 成立并已修复；第 8 条（存储配额）经实测不成立。

- [x] **富 HTML 视频全屏按钮按场景控制**：`allowsFullscreenVideo` 提为 prop，列表预览默认关闭，仅全屏 Modal 传 `true`，视频不再跳出聊天上下文。
- [x] **视口卡片高度与宿主脱节**：新增 `hostHeight` prop 与 `resolveViewportCardHeight` 纯函数；Modal 用 `onLayout` 实测高度，列表预览按屏幕比例估算并统一封顶 520，长屏手机不再挤占聊天区。
- [x] **全屏卡片 Modal 安全区与状态栏**：顶栏 `paddingTop` 取安全区高度；`viewportCardBody` 去掉 `justifyContent: 'center'`；Modal 补 `statusBarTranslucent` + `presentationStyle="fullScreen"`，避免刘海遮挡与安卓黑边。
- [x] **全屏时列表内双 WebView**：`cardFullOpen` 期间预览分支渲染等高静态占位，关闭 Modal 后原位重建；视口卡高度为确定性计算，重建不跳变。
- [x] **预览完全不可交互**：去掉 `pointerEvents="none"`，用透明覆盖层把整块卡片变成“点按进全屏”，保留“全屏交互”按钮作视觉提示。
- [x] **正则/世界书静默跳过无提示**：新增 `getUnsafeWorldEntryKeys`；正则列表、世界书列表及两处编辑表单对疑似灾难性回溯的脚本/关键词显示“运行时会跳过”提示。
- [x] **滚动阈值 24000 过于极端**：拆分 `RICH_HTML_MAX_RENDER_HEIGHT`（渲染上限 24000）与 `RICH_HTML_SCROLL_THRESHOLD`（滚动阈值 6000）；普通富 HTML 超过 6000px 时卡片收成 480 固定高度并允许内部滚动，不再整块撑满聊天列表。
- [~] **存储配额 887M 写满**：不成立。实测 `node_modules` 408M 且被 git 忽略，`/tmp/opencode` 下其余为 KB 级，磁盘 14G 可用，不会阻断同步与安装；无需处理。

## 本轮逐行审计新增发现

> 本节记录本轮按五类问题逐文件追踪调用链后确认的待办。每项在修复前都保留证据、依赖关系和回归测试要求；修复顺序按“共享底层 → 调用方 → UI 表现”排列，避免一个修复制造另一个问题。

### 1. 逻辑、状态与持久化问题

- [x] **P1｜摘要作用域使用陈旧 preview**：`src/memorySummary.js:165-177` 只按 `session.preview` 计数；`src/storage.js:2499-2511` 写盘后未同步 `sessionsRef`，第二个会话可能把摘要写入世界书。修复需增加可靠消息存在标记或刷新来源，并覆盖空 preview、写入失败、会话删除后的计算。已改为计入活动会话消息与 `summarizedUpTo`，并补充回归测试。
- [x] **P1｜摘要双写无补偿**：`src/memorySummary.js:300-317` 先写世界书摘要，再写会话 boundary；第二步失败会留下启用中的孤儿摘要。修复需使用唯一 entry id、revision 校验和精确补偿或 journal，禁止用整份旧 `worldInfo` 覆盖并发编辑。已使用稳定 entry id 并在 boundary 写失败时精确补偿，避免整份覆盖。
- [x] **P1｜重新生成/修改重发/清空没有统一失效摘要**：`src/ChatScreen.js:1767-1800`、`3084-3209`、`3211-3248`；旧消息被替换或清空后仍可能注入旧摘要。需要抽出共享历史变更事务，覆盖 scoped 与 unscoped。已抽出 `invalidateHistorySummaries`/`planSummaryInvalidation` 并在批量删除、清空、修改重发、重新生成接线。
- [x] **P1｜批量删除没有统一提交点**：`src/ChatScreen.js:3335-3372` 先停用世界书摘要、再重置会话摘要、消息体最后由 effect 保存；任一步失败都会造成消息与记忆不一致。需要 journal、补偿或可恢复状态。已改为边界先提交、摘要按边界精确失效并可回滚。
- [x] **P2｜消息自动保存过早更新成功快照**：`src/ChatScreen.js:1687-1753` 在写入前设置 `lastSavedSnapshotRef`；一次性写盘失败后依赖不变，不会自动重试。已改为写盘成功后才推进快照，并加入有界退避重试。
- [x] **P1｜损坏用户资料可能导致头像误回收**：`src/storage.js:147-153`、`1349-1386`、`2014-2024`；资料读取异常被当成空资料，回收器无法知道用户头像引用仍存在。任何引用源损坏时应停止回收。已增加 `getUserProfileStatus` 与备份守卫，损坏时整体跳过回收。
- [x] **P1｜群聊创建存在跨键和导航竞态**：`src/storage.js:2798-2804`、`src/CharacterScreen.js:1207-1232`；已增加 sessions/activeId 回滚，并在 Context 刷新成功后才导航；补充存储回归测试。
- [x] **P2｜批量删除后持久化 activeSessionId 仍指向已删除会话**：`src/storage.js:3019-3055`；已同步更新活动会话指针，删除当前会话时切到剩余会话或清空，并补充回归测试。
- [x] **P2｜recordTurn 后台写可跨会话归属并覆盖 affinity**：已确认 moment 使用发送时快照 `senderSnapshot.sessionId`；affinity 读-改-写已用 promise 队列串行化，连续回复不再互相覆盖增量。
- [ ] **P2｜启动迁移与 refreshSessions 可互相覆盖**：`App.js`、`src/context/AppContext.js:315-329`；外部 `refreshSessions` 已改为进入 mutation 队列，避免与 Context 写入交错；但存储层迁移不在同一队列，迁移进行中的刷新仍可能读到中间态，待统一调度。
- [x] **P2｜会话回滚备份只写不读**：`src/storage.js:2386-2425`；读取会话列表损坏时优先用 `@easychat2_sessions__rollback_backup` 恢复并清理备份，回滚成功时也清理，补充启动恢复测试。
- [x] **P2｜媒体 revision 保护可能造成延迟泄漏**：`src/mediaProtection.js`、`src/storage.js:2508-2535`；已新增 `getNextRecentMediaExpiry`，回收器跳过近期文件后会在保护窗口结束时自动重跑一次，并补充回归测试。
- [x] **P2｜孤儿会话扫描无界并发读取大消息**：`src/storage.js:3029-3032`；大量大消息会并发打开 SQLite 并持有完整字符串，需要限制并发并隔离单条失败。已改为每批最多并发 4 个消息体。
- [ ] **P2｜向量索引仍使用单键且缺少模型指纹**：`src/vectorMemory/index.js:16-34`、`294-320`、`src/storage.js:894-906`；已补 `vectorSignature`（provider/baseUrl/model/maxChars），配置变化时旧向量会被重嵌且不再参与余弦检索；单键存储仍未分片，大库读取上限待处理。
- [x] **P2｜本地历史搜索旧请求继续消耗大值读取**：`src/SearchScreen.js:56-95`、`src/storage.js:2690-2729`；token 只阻止旧结果回写，底层循环没有取消信号。`searchMessages` 已支持 `AbortSignal`，输入变化、关闭、选中结果和卸载都会中止旧搜索，并补充取消回归测试。
- [x] **P2｜同会话自动保存并行写盘可回退**：`src/ChatScreen.js:1691-1791`；两个快照并行落盘时旧快照可能覆盖新结果。已用 promise 队列串行化同会话写盘。

### 2. 未命名变量、参数与调用链问题

- [x] **P1｜Chat Completions 数组 content 未规范化**：`src/api.js:98-104`、`408-413`；已统一字符串/content-parts 解析，并补充流式、非流式回归测试。
- [x] **P1｜TTS 并发 speak 共享 generation**：`src/tts/index.js:382-467`；已增加独立请求代号，跨 `await stopPlayback()` 的旧调用会失效，并发回归测试通过。
- [x] **P2｜CardForge 同步答题入口未校验当前 questionId**：`src/CardForgeScreen.js:151-160`、`src/cardForge/forge.js:218-244`；已校验当前问题并补充重复提交测试。
- [x] **P2｜生图模型值在检测、试生成和正式生成间不一致**：`src/ImageGenScreen.js:133-140`、`209-218`、`245-254`、`399`；逗号/换行多模型会发送非法完整字符串。已统一 `normalizeModelList` 去重并选取首个模型，测试通过。
- [x] **P2｜辅助请求遗漏 API 配置切换保护**：`src/CardForgeScreen.js:143-148`、`src/MomentsView.js:231`；制卡和动态回复均读取并传递配置 ID/指纹。
- [x] **P2｜群聊开场接口参数未使用**：`src/groupChat.js:219-263`；已把 userProfile persona 与 globalPresets 注入开场提示词，并补充回归测试。
- [x] **P2｜动态回复旧 finally 可清理新 controller**：`src/MomentsView.js:259-264`；已改为仅当当前注册的 controller 与本次一致时才删除。
- [x] **P2｜动态评论保存期间的新草稿会被旧请求清空**：`src/MomentsView.js:288-305`；已按提交文本校验，只有草稿未被改动时才清空。
- [x] **P2｜角色/群聊编辑弹窗保存期间仍可输入**：`src/CharacterEditForm.js`、`src/GroupEditForm.js`；已在整个表单区域封锁滚动与指针事件，避免保存快照之外的输入被关闭丢弃。
- [x] **P3｜主题保存失败静默且存在旧闭包写回**：`src/theme/ThemeContext.js`；已使用最后成功快照、串行保存队列，失败时回滚并提示。

### 3. 大角色卡运行、渲染与性能问题

- [x] **P1｜大卡进入 AI 制卡后长字段被默认 4000 截断**：`src/cardForge/forge.js:132-147`、`250-257`、`338-355`；投影、解析和合并改用 500,000 字符保留上限，并补充大字段回归测试。
- [x] **P1｜每个流式 token 重跑全部历史展示正则**：`src/ChatScreen.js:1444-1477`、`4202-4325`；大卡 500 条长消息可产生明显主线程卡顿。已新增 `src/displayTextCache.js`，未变化的历史消息按消息对象缓存展示结果，仅当前流式消息仍会重算；正则执行时间预算仍待补。
- [x] **P1｜深度世界书可能插入 system 之前**：`src/chatPipeline.js:35-58`；插入下标已钳制到至少 1，system 始终在最前，并补充回归测试。
- [x] **P1｜导入上限与 PNG 导出不闭合**：`src/CharacterScreen.js:61-62`、`942-965`、`src/cardExporter.js:264-346`；应用可生成超过自身 32MiB 导入上限的 PNG。已新增 32 MiB 上限校验，超过时明确报错。
- [x] **P2｜空 systemPrompt 导出后组合提示重复**：`src/cardExporter.js:278`、`src/cardParser.js:470-527`；已保留空原始字段并补充导出回归测试。
- [x] **P2｜扩展字段 boundary 往返不一致**：`src/cardExporter.js:204-233`、`src/cardParser.js:152-226`；已同时支持顶层与 extensions boundary，并补充往返断言。
- [x] **P2｜JPEG 头像导出会静默替换为占位 PNG**：`src/CharacterScreen.js:1665-1687`、`src/cardExporter.js:319-327`。已改为非 PNG 头像直接报错，不再生成错误卡片，并补充回归测试。
- [ ] **P2｜大卡 824 条世界书/19 条正则列表仍一次性挂载**：`src/CharacterScreen.js:2231-2263`；需要虚拟列表或分页低端机验证。
- [x] **P2｜HTML 围栏剥离会删除 `<pre>` 内合法代码围栏**：`src/richHtml.js:9-15`、`207`；已保护 pre/code 内容并补充测试。
- [x] **P2｜viewport 样式识别误判转义代码示例**：`src/richHtml.js:150-156`；已限定真实 HTML 标签 style 属性并补充测试。
- [x] **P2｜角色卡扩展字段白名单造成 V3 往返丢失**：`src/cardParser.js`、`src/CharacterScreen.js`、`src/cardExporter.js`；已新增 `extensions`/`extra` 透传桶，保留未知扩展（talkativeness/fav/depth_prompt 等）与顶层字段（creator/character_version 等），导出时写回，并补充往返测试。

### 4. 其他功能、配置与平台问题

- [x] **P1｜第三方角色卡正则缺少执行隔离/时间预算**：`src/regexEngine.js:9-10`、`128-159`、`src/ChatScreen.js:1434-1464`；灾难性回溯会阻塞 JS 主线程。已新增嵌套无界量词启发式识别，命中即跳过，世界书正则同步拦截，并补充回归测试；签名级时间预算仍待后续 worker 方案。
- [x] **P1｜CI 使用可变 EAS CLI**：`.github/workflows/build-apk.yml:27` 已固定 `eas-cli@24.8.0`，不再随上游漂移。
- [ ] **P2｜CI 的 actions/* 仍为大版本 tag**：`actions/checkout@v4`、`actions/setup-node@v4`、`actions/setup-java@v4`、`actions/upload-artifact@v4` 尚未固定到 commit SHA，存在供应链漂移风险。
- [ ] **P2｜TTS 讯飞 WebSocket/腾讯签名契约未实现**：`src/tts/providers.js:60-122`、`src/tts/index.js:90-140`；XHR 不能处理 wss，腾讯 signer 未生成 Authorization。
- [x] **P2｜TTS 设置保存失败回滚到未保存草稿**：`src/TtsPanel.js:51-72`；聊天页播报开关保存失败也未回滚。已加入保存队列、最后成功快照和失败回滚，聊天页开关同步回滚。
- [x] **P2｜联网搜索保存队列会用旧结果覆盖新编辑**：`src/PluginPanel.js:52-97`；保存失败时开关状态也不回滚。已加入保存版本号、最后成功快照与失败回滚。
- [x] **P2｜生图设置保存失败会关闭面板并隐藏草稿**：`src/ImageGenScreen.js:165-181`、`300-323`。已改为仅在保存成功后关闭面板。
- [x] **P2｜生图旧配置结果可写入新配置画廊**：`src/ImageGenScreen.js:360-423`；请求缺少 provider/config revision。已加入配置指纹校验与生成中止。
- [ ] **P2｜富 HTML 脚本可无限执行且远程资源信任边界未定**：`src/richHtml.js:166`、`src/RichHtmlMessage.js:165-177`。
- [x] **P2｜搜索结果 FlatList key 可碰撞且消息缺失 id 时不稳定**：`src/SearchScreen.js:167`；已改为 `sessionId-messageId-index` 保证唯一。
- [x] **P3｜生图结果下载无超时且固定 PNG MIME/扩展名**：`src/ImageGenScreen.js`、`src/imageResultFormat.js`；下载已加 60 秒超时，扩展名与分享 MIME 按 mimeType/URL 后缀解析，并补充单测。
- [x] **P3｜主题、设置等保存失败提示和最后成功快照不统一**：`src/theme/ThemeContext.js`；主题/TTS/联网搜索/生图面板均已统一为串行保存 + 最后成功快照 + 失败回滚提示。
- [x] **P2｜会话列表损坏时启动迁移可能无限静默重试**：`App.js:157-210`；已限制为最多 5 次，超过后停止重试并提示用户检查存储。
- [ ] **P2｜iOS 允许保存 HTTP API，但 release 缺少 ATS 契约处理**：`src/SettingsScreen.js:796-818`、`src/api.js:173-175`、`app.json:52-56`。

### 5. 仍需真机或产品确认

- [ ] 压缩 iTXt/zTXt PNG 元数据解析方案。
- [ ] 超大 HTML、824 条世界书、两个 WebView 同时挂载时的低端机内存和滚动。
- [ ] 远程图片/视频默认策略、原生命令桥和 CSP 兼容性。
- [ ] 讯飞/腾讯/火山 TTS 真实服务协议与签名。
- [ ] 重复 session ID、多个 builtin 角色、群像普通文本回退。
- [ ] Android SQLite fallback、进程终止窗口、文件回收和正式 release 签名。

## 已确认待处理

- [ ] 富 HTML 页面脚本、远程图片/视频、命令桥的信任边界；当前功能仍允许卡片脚本主动构造原生消息
- [ ] 富 HTML 外部导航、`window.open`、多窗口 WebView 的统一拦截策略
- [ ] 富 HTML 原有 CSP 与运行时 CSP 冲突时的真机兼容策略
- [ ] 压缩 iTXt PNG 角色卡元数据解析；需要确定 React Native 可用的 zlib 实现或依赖
- [ ] 超长非视口 HTML 在低端设备上的滚动与内存验证；滚动阈值已从 24000 下调至 6000，超阈值内容收成 480 固定高度内部滚动，待真机验证嵌套滚动手感
- [ ] TTS 讯飞/腾讯签名算法、令牌错误提示与服务商实测
- [ ] 向量索引按 session/segment 分片，模型指纹、索引迁移与容量上限
- [x] **向量索引更新在切会话/卸载时的 AbortController 生命周期**：`src/ChatScreen.js` 保存 effect 每次运行创建 `AbortController`，切会话/卸载/下一次保存时中止嵌入，向量不落半成品。
- [x] **重复 session ID 的消息键迁移或阻断恢复策略**：`src/storage.js:2376-2390`；重复 id 现在保留首条、丢弃重复条目，避免重命名后孤立消息键，并补充回归测试。
- [x] **多 builtin 角色跨排序后的 default 身份稳定性边界**：`src/storage.js:281-301`；只允许 default 持有 builtin，其余清标记，并补充回归测试。
- [ ] 群像模型返回普通文本时的保留/回退契约
- [ ] 聊天主列表的全量 ScrollView 虚拟化与 WebView 可见性挂载策略；全屏打开时列表内已不再保留第二份 WebView，主列表虚拟化仍未做
- [x] **摘要重置与世界书禁用化仍是两次独立写入，失败时需要 journal/补偿**：`src/memorySummary.js:262-311`；禁用前记录条目原始开关，会话摘要写失败时按原值精确回滚，并补充补偿回归测试。
- [ ] 会话 activeId 与会话列表的跨键事务；当前已有回滚，持续磁盘失败需恢复工具
- [x] **启动文件回收与新媒体写入的统一互斥队列**：`src/storage.js` 已将 `collectChatImageFiles` 排入单条 promise 队列，任意时刻只有一次回收在跑，新媒体写入仍受 revision 失效保护。
- [ ] 依赖漏洞与 Expo 工具链升级；先完成兼容性验证
- [x] CI 固化 no-undef、ESLint 与 Android export 门禁
- [ ] CI 继续补充 prebuild、Action 版本固定与依赖审计门禁
- [x] **文档、规格、接口导出与当前实现同步**：已同步 `.monkeycode/docs/审查待办.md`、`INTERFACES.md`（新增 `vectorSignature`、`resolveImageFormat`、`normalizeModelList`、`getCachedDisplayText`、`invalidateHistorySummaries`、`getNextRecentMediaExpiry`、扩展透传）、`ARCHITECTURE.md` 模块树与 `AGENTS.md` 的 lint 命令。

## 需要产品确认

- [ ] 联网搜索外部媒体是否默认允许；当前交互卡片依赖 HTTPS 图片和视频
- [ ] 世界书 position 1/2/3/5/6/7 的完整插入语义；当前文档定义的非深度位置统一拼入 system 前后
- [ ] 群像普通文本回复是否保留原文，当前解析器只接受“角色名：内容”格式
- [ ] 内部 preview APK 是否继续使用 debug 签名，公开分发是否需要正式 keystore

## 已排除的误报

- [x] SQLite `substr` 固定 `chunkSize` 的 Unicode 偏移错误：当前 offset 使用固定字符步长，位置计算正确
- [x] `@全体` 被 `MAX_SPEAKERS` 截断：`selectSpeakers` 在 `everyone` 分支直接返回全部成员
- [x] `makeCharacterPresetId(now)` 忽略 now：当前实现已经使用传入的 now
- [x] 世界书 position 2/3/5/6/7 当前统一注入 system 前后：与项目世界书文档契约一致
- [x] `scoped=true` 时忽略世界书摘要：这是会话隔离契约；非 scoped 已合并两类摘要
- [x] Node ESM 无扩展名导入：Metro/React Native 解析路径正常，属于测试工具兼容性
- [x] 缺少未定义变量：当前 Babel AST 与 ESLint 检查均无输出

## 验证门禁

- [x] `npm test`（当前 206 项通过）
- [x] `npx eslint --config /tmp/eslint.check.mjs App.js src/*.js src/*/*.js`
- [x] `npm run lint`（仓库内固定 ESLint 配置）
- [x] `npx expo export --platform android`（最终导出成功）
- [x] `node` YAML/JSON 配置解析
- [ ] Android 真机：首启流程、角色卡导入、富 HTML 滚动、搜索取消、TTS、动态切换、文件回收
- [ ] 用户检查完成后，再按主题拆分提交并推送
